## 主页面

Bug:

1. 目前的窗口移动方案是, 先显示窗口，再使用 moveWindow 移动窗口，这回导致在电脑性能较低的时候，这样移动的过程很明显的闪了一下。

## 设置页面 UI

### 快捷键设置

Bug:

1. 当按下 Ctrl + Shift + .. ，如果冲突，则错误的 UI 会显示上档符号，例如 "Ctrl + Shift + <" ，而不是 "Ctrl + Shift + ,"，导致有迷惑性。